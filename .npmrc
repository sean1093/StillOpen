# Make the "engines" range in package.json a hard install gate, not a warning.
# The range is the intersection of what the dependency tree actually requires:
# vite/rolldown need ^20.19.0 || >=22.12.0, vitest needs ^20 || ^22 || >=24.
engine-strict=true
